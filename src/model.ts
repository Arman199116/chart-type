export type DataType = {
    market_caps: [
        Array<number>[]
    ],
    prices: [
        Array<number>[]
    ],
    total_volumes: [
        Array<number>[]
    ],
}

export interface ArrType {
    key : number,
    span : string,
    ul : string
}